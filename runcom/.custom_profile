#!/usr/bin/env bash

# shellcheck source=/dev/null
. "$HOME/dotfiles/runcom/.functions"
# shellcheck source=/dev/null
. "$HOME/dotfiles/runcom/.env"
# shellcheck source=/dev/null
. "$HOME/dotfiles/runcom/.aliases"
# shellcheck source=/dev/null
. "$HOME/dotfiles/funcs.sh"

# shellcheck source=/dev/null
source_recursive "$HOME/dotfiles/tools"

