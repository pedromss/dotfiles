alias c="clear"
alias l="colorls -lA --sd"
alias ld="ls -ldh"
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias brc="vim ~/.bashrc"
alias activemq='docker run --name='activemq' -it --rm -e 'ACTIVEMQ_MIN_MEMORY=1024' -e 'ACTIVEMQ_MAX_MEMORY=2048' -p 61616:61616 -p 8161:8161 -p 5672:5672 -p 61613:61613 -p 1883:1883 -p 61614:61614    webcenter/activemq:latest'
alias jenkins='docker run -p 8080:8080 -p 50000:50000 -v jenkins_home:/var/jenkins_home -v $(which docker):/usr/bin/docker jenkins/jenkins:lts'
alias cdocker='docker-compose'
alias ydl='youtube-dl -x --audio-format mp3 --audio-quality 0 --no-playlist -o "$HOME/Music/youtube-dl/%(title)s.%(ext)s"'
alias ydl-list='youtube-dl -x --audio-format mp3 --audio-quality 0 --yes-playlist -o "$HOME/Music/youtube-dl/%(title)s.%(ext)s"'

# AWS
alias awscf='aws cloudformation'

# PMD
pmd_version=6.0.1
alias pmd="$HOME/pmd-bin-$pmd_version/bin/run.sh pmd"
alias cpd="$HOME/pmd-bin-$pmd_version/bin/run.sh cpd"

# Gradle
alias gw="gw --console=plain"
alias gradle="gradle --console=plain"

# Git
alias gl="git pull"
alias gp="git push"

